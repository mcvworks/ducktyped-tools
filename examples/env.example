# Copy this file to .env in your application's expected config directory.
# Replace API_TOKEN locally; the empty value below is only a placeholder.
# Rename these variables to match your application's documentation.
APP_ENV=development
API_BASE_URL=https://api.example.com
API_TOKEN=
PORT=3000
LOG_LEVEL=info
